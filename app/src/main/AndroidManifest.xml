<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.shahed.firebace">
    <!--
         The ACCESS_COARSE/FINE_LOCATION permissions are not required to use
         Google Maps Android API v2, but you must specify either coarse or fine
         location permissions for the "MyLocation" functionality.
    -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.WereToGo">
        <activity
            android:name=".views.OrderDoneActivity"
            android:exported="false" />
        <activity
            android:name=".views.DetailsEventActivity"
            android:exported="false" />
        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/.
        -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_maps_key" />

        <activity
            android:name=".CompanyUI.MapsActivity"
            android:exported="false"
            android:label="@string/title_activity_maps" />
        <activity
            android:name=".CompanyUI.AddEventActivity"
            android:exported="false" />
        <activity
            android:name=".CompanyUI.MyEventsActivity"
            android:exported="false" />
        <activity
            android:name=".CompanyUI.MainActivity"
            android:exported="false" />
        <activity
            android:name=".CompanyUI.CategorieActivity"
            android:exported="false" />
        <activity
            android:name=".views.EventsActivity"
            android:exported="false" />
        <activity
            android:name=".views.EditEventActivity"
            android:exported="false" />

        <service android:name=".utils.NotificationsService" />

        <activity
            android:name=".views.MainActivity"
            android:exported="true" />
        <activity
            android:name=".WelcomeActivities.SplashScreenActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".WelcomeActivities.WelcomeActivity"
            android:exported="true" />

        <activity
            android:name="com.adevinta.leku.LocationPickerActivity"
            android:exported="false"
            android:label="@string/leku_title_activity_location_picker"
            android:parentActivityName=".CompanyUI.AddEventActivity"
            android:theme="@style/Theme.MaterialComponents.Light.NoActionBar"
            android:windowSoftInputMode="adjustPan">
            <intent-filter>
                <action android:name="android.intent.action.SEARCH" />
            </intent-filter>

            <meta-data
                android:name="android.app.searchable"
                android:resource="@xml/leku_searchable" />
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value=".CompanyUI.AddEventActivity" />
        </activity>
    </application>

</manifest>